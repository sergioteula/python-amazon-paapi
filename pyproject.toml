[project]
name = "python-amazon-paapi"
version = "7.0.0"
description = "Amazon Creators API wrapper for Python"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [{ name = "Sergio Abad", email = "sergio.abad@bytelix.com" }]
keywords = ["amazon", "creators-api", "affiliate", "api"]
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "certifi>=2023.0.0",
    "pydantic>=2.0.0",
    "python_dateutil>=2.8.0",
    "requests>=2.28.0",
    "urllib3>=1.26.0,<3",
]

[project.urls]
Homepage = "https://github.com/sergioteula/python-amazon-paapi"
Repository = "https://github.com/sergioteula/python-amazon-paapi"

[project.optional-dependencies]
async = ["httpx>=0.27.0", "typing-extensions>=4.15.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["amazon_creatorsapi", "creatorsapi_python_sdk"]

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pre-commit>=2.21.0",
    "pytest>=7.4.4",
    "pytest-cov>=4.1.0",
    "python-dotenv>=1.2.1",
    "ruff==0.14.11", # Keep in sync with the ruff rev in .pre-commit-config.yaml
    "types-requests>=2.32.4.20260107",
]

[tool.ruff]
target-version = "py39"
cache-dir = ".cache/ruff"
exclude = ["creatorsapi_python_sdk/*", "docs/*"]

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "ANN003", # Missing type annotation for **kwargs
    "ANN401", # Dynamically typed expressions (typing.Any) are disallowed
    "D203",   # 1 blank line required before class docstring (conflicts with D211)
    "D213",   # Multi-line docstring summary should start at the second line (conflicts with D212)
    "COM812", # Missing trailing comma (conflicts with Ruff formatter)
    "ISC001", # Implicitly concatenated string literals (conflicts with Ruff formatter)
    "PT019",  # Fixture without value is injected as parameter
]
exclude = ["creatorsapi_python_sdk/*", "docs/*"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = [
    "ANN201", # Missing return type annotation for public function
    "ANN206", # Missing return type annotation for classmethod
    "ARG002", # Unused method argument (mock parameters are ok)
    "D",      # pydocstring rules (no docstrings required for tests)
    "PT009",  # Use a regular assert instead of unittest-style
    "PT027",  # Use pytest.raises instead of unittest-style
    "S101",   # Use of assert detected (assert is fine in tests)
    "S105",   # Possible hardcoded password (test credentials are ok)
    "S106",   # Possible hardcoded password argument (test credentials are ok)
    "SIM117", # Multiple context managers (nested with is ok in tests)
    "SLF001", # Private member accessed
]
"scripts/*" = [
    "T201", # print found (CLI scripts use print)
]
"**/api.py" = ["PLR0913"] # Too many arguments to function call

[tool.ruff.format]
exclude = ["creatorsapi_python_sdk/*", "docs/*", ".github"]

[tool.mypy]
# The package supports Python 3.9, but mypy only checks 3.10 and above, so the
# lowest version it can be told about is the one used here
python_version = "3.10"
ignore_missing_imports = true
no_implicit_optional = true
strict_equality = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_configs = true
warn_unused_ignores = true
cache_dir = '.cache/mypy'
exclude = ["creatorsapi_python_sdk/*"]

[tool.coverage.run]
branch = true
relative_files = true
source = ["amazon_creatorsapi"]

[tool.coverage.report]
precision = 2
skip_covered = true
skip_empty = true
fail_under = 98

[tool.coverage.html]
directory = "coverage_html_report"
skip_covered = false

[tool.pytest.ini_options]
testpaths = "tests"
cache_dir = ".cache/pytest"
